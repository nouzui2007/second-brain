# Mock起動

# 前提

Dockerがインストール済みである。

## 実行方法

yamlがあるディレクトリで以下のコマンドを実行する。

```
docker run --init --rm -v "$(pwd)":/tmp -p 4010:4010 stoplight/prism:4 mock -h 0.0.0.0 "/tmp/jrshikoku-ticketless.yaml"

```