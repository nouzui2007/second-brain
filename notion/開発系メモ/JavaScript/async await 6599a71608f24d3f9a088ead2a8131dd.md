# async / await

```jsx
const promise = (i) => new Promise(
  (resolve, reject) => {
    setTimeout(
      () => {
        if (i > 5) {
          resolve("Power")
        } else {
          reject("Not Power")
        }
      }, 1000
    )
  }
)

const firstStep = async (random) => {
  const res01 = await promise(random).then(message => console.log(message)).catch(error => console.log(error))
  const res02 = await promise(random).then(message => console.log(message)).catch(error => console.log(error))
  const res03 = await promise(random).then(message => console.log(message)).catch(error => console.log(error))
}

const Random = () => {
  let r = Math.random() * 10;
  console.log(r)
  return r
}

firstStep(r)
```