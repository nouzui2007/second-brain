# Tips

## 認証されているか

```jsx
firebase.auth().onAuthStateChanged((user) => {
        if (user) {
            console.log("ログイン状態")
        } else {
            console.log("ログインしていない状態")
            return redirect('/signin')
        }
    })
```