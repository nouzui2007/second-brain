# 正規表現で文字列操作

## Bearerで始まる文字列（JWTトークン）からBearerを削除

Bearer xxxxxxのような文字列から、Bearerを外してトークン部分だけを取り出す。

```scala
val reg = """Bearer (.+?)""".r
val token = Some("Bearer hogehogehogehoghohohohoho")
token.collect {
  case reg(token) => token
}
```